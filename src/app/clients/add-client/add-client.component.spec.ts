
import { AddClientComponent } from './add-client.component';

describe('Component: AddClient', () => {
  it('should create an instance', () => {
    let component = new AddClientComponent();
    expect(component).toBeTruthy();
  });
});
